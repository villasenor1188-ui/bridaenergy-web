export const metadata = { title: 'BRIDA Energy', description: 'Energía solar profesional' }
export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  )
}
